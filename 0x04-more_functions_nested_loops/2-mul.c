#include "main.h"

/**
 * mul - A function that multiples two numbers
 * @a: first input of the function
 * @b: second input of the function
 * Return:returns multiplication result of the 2 inputs
 */

int mul(int a, int b)
{
	return (a * b);
}
